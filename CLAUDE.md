# 開発の決まりごと

個人用 AI チャット Web UI。React Router v8（Framework Mode）+ React 19 +
TypeScript を Cloudflare Workers に載せ、D1・R2・Durable Objects を使う。

仕様は `docs/requirements.md` が唯一の出所。**振る舞いを変えたら、同じ変更で
要件も直す。** 直せない事情があるなら、そのことを要件に書く。

## テストの書き方

このリポジトリの品質は、テストの**数**ではなく「そのテストが実際にバグを
捕まえるか」に懸かっている。過去に7回、書いた直後は正しく見えるのに何も
検査していないテストを書いた。以下はその実例から出た決まりごと。

### 書いたら必ず壊して確かめる

テストを足したら、**検査対象のコードにわざと誤りを入れて、落ちることを見る**。
落ちなければ、そのテストは通っているだけで何もしていない。実際に起きたもの:

- 表の比較で `every` を `some` に変えても全部通った（順序が問題になる例が
  1つも無かった）。
- 数式の分割ガードのテストが、閉じていない数式を全部末尾側に置いていた。
  分割点がガードの内側に落ちる例が無く、ガードを消しても通った。
- 2つのリセット処理が互いに冗長で、片方ずつ消しても差が出なかった。

押さえきれない経路（到達不能・等価な分岐）があるなら、**その旨をコミットに
書き残す**。黙って諦めない。

### SQL は本番と同じものを走らせる

問い合わせをテストへ**書き写さない**。`app/lib/schema.ts` から export された
文字列を、production とテストの両方が使う。

書き写したせいで危うくバグを出した実例がある。画像一覧の問い合わせを写すとき
JOIN を落とした。写した側は通ったが、本番の SQL は `HAVING created_at < ?` が
JOIN のせいで曖昧列となりエラーになる——**画像一覧が丸ごと開かなくなる**
状態でテストは緑だった。

スキーマとマイグレーションは `node:sqlite`（`DatabaseSync`）に実際に流す。
「SQLite はこう解釈するはず」という推測を挟まない。

### 「無いこと」だけを見ない

`queryByText(...)` が null であることは、**畳まれているとき**にも
**コンポーネントが落ちているとき**にも成り立つ。必ず「畳まれている隣で、
親の要素は見えている」まで書く。

### 出力を読まないと分からないものがある

Tailwind はクラス名を間違えても黙って規則を出さない。`dark:[@media(hover:none)]:`
は、生成された CSS ではメディアクエリの**外**に出ていた（＝デスクトップの
ダークテーマにも当たる）。ソースを何度読んでも分からず、ビルド結果を読んで
初めて見つかった。似たものは `tests/touch-variant.test.ts` の形で、
**出力そのものを検査する**。

同じ理由で、CSP のような「ブラウザが解釈して初めて効く」ものは、実際に
ブラウザで開いて確かめる。Chromium は `/opt/pw-browsers/chromium` にあり、
playwright は `/opt/node22/lib/node_modules` にある。

### 静かに壊れる結び付きにはガードを置く

片方を変えるともう片方が黙って効かなくなる組み合わせは、テストで結び付きを
見張る（例: `tests/csp.test.ts` の「CSP の配線」）。画面にエラーが出ない
壊れ方は、気づく手段を先に用意しておかないと見つからない。

## テストの層

`vitest.config.ts` に5つの project がある。置き場所で走らせ方が決まる。

| 置き場所 | 名前 | 環境 | 何のため |
|---|---|---|---|
| `tests/*.test.ts` | node | jsdom | 純粋な変換・判定・組み立て |
| `tests/dom/*.test.tsx` | dom | jsdom + setup | 画面の操作（送信・編集・分岐・削除） |
| `tests/server/*.test.ts` | server | node | `cloudflare:workers` を差し替え、バインディングに触らない経路 |
| `tests/schema.test.ts` | schema | node | 本物の SQLite にスキーマと問い合わせを流す |
| `tests/touch-variant.test.ts`<br>`tests/built-css.test.ts` | css | node | ビルド後の CSS を読む |

サーバー側のモジュールは `tests/stubs/cloudflare-workers.ts` で `env` を
差し替えている。`env` に触ると例外を投げる Proxy なので、「バインディングに
触らないはず」の経路が実は触っていれば、その場で分かる。

## Cloudflare の制約

- **サブリクエスト**: 1回の呼び出しで外部 fetch 50件・内部（D1・R2・他の DO）
  1,000件（無料プラン）。使い切ると以降が全部失敗する——**途中経過だけでなく
  最後の確定も**通らなくなり、受け取った本文がどこにも書かれずに消える。
  長い生成では書き込み間隔を段階的に広げる（`app/lib/flush-cadence.ts`）。
  何度も呼ぶ実行では**数えて手前で区切る**（`RETRY_CHUNK_INTERNAL_LIMIT`）。
  数えていなかったとき、「成功するまで生成」が枠を使い切って見出しを
  打ち直せなくなり、中断とみなされて黙って終わった。
- **D1 のバインド変数は1文へ100個まで**。会話が開けない・消せないという形で
  出たことがある。まとめて流すときは必ず分割する。
- `d.batch()` は全体で1サブリクエストとして数えられる。
- **Durable Object は「実行体1つが起きている時間」で課金される**。外の応答を
  待っているあいだも含む。無料枠は1日 13,000 GB秒＝128MB 換算で約104,000秒。
  依頼1本ごとに実行体を分けると、待ち時間が**並列数だけ倍に**課金される——
  368本の実行1回で日の枠の6割を使い切り、以後どの生成も始められなくなった。
  1つの実行体の中で同時に投げれば待ち時間は1本ぶんで済むので、担当1つに
  依頼をまとめて持たせる（`RETRY_WORKER_ATTEMPTS`）。並列を増やしたいときに
  実行体を増やすのは、費用を並列数倍にすることと同じ。
  **この消費はどこの台帳にも載らない。**上流の課金と違い、Cloudflare からの
  通知メールが来るまで気づけず、来たときにはもう締め出されている。自分で
  数えて手前で止めるしかない（`DAILY_DO_MS_SQL`）。数える区切りは UTC の0時
  ——枠が戻る時刻で、JST の暦日ではない。
- **締め出されているあいだのアラームは消えない。**枠が戻った瞬間に一斉に
  動き出す。実際に、戻ってから30分でまた1日分を使い切った。外からアラームを
  消す手立ては無いので、**司令役が毎秒見る停止の印**を立てて止める
  （設定画面の「すべて止める」）。
- **Durable Object のアラーム1回は最長15分**（壁時計）。ここを過ぎると実行ごと
  止められ、返事待ちの上流の依頼は失われる（課金は済んでいる）。
- **応答ヘッダを同時に待てる接続は1回の呼び出しで6本まで**。Poe は画像が
  できるまでヘッダを返さないので、7本目以降はこちらで順番待ちになる。
- どちらも「1回の呼び出しあたり」の制限なので、「成功するまで生成」は
  依頼1本ごとに別の Durable Object へ渡す（`app/lib/retry-run.server.ts`）。
  1本の締め切り（12分）は担当の実行の壁の手前。司令役は依頼を抱えないので、
  区切られても何も失わない。並列の上限は上流のレート制限と利用者の設定だけ。

## 変更を出す前に

```
npm run typecheck    # 型（tsc -b。--noEmit ではない——下記）
npx eslint .         # エラー0（警告20はフック関連で既知）
npx vitest run       # 全通過
npm run build        # ビルド
```

4つ全部が通ってから push する。CI も同じものを見る。

**`npx tsc --noEmit` は何も検査しない。** ルートの `tsconfig.json` は
`"files": []` と references だけで、`tsc` は単体では references を辿らない
（辿るのは `-b`）。そのため終了コード0で即座に返る——型エラーがあっても
「通った」ように見える。実際、この空振りに気づかないまま作業して、
import を書き忘れた19ファイルが素通りした。型を見るときは
`npm run typecheck`（`wrangler types && react-router typegen && tsc -b`）を
使う。ルートの `./+types/*` は typegen が作るので、その前段も要る。

## 書き方の癖

- コメントと文章は日本語。**何をしているか**ではなく、**なぜそうなっているか**
  ——特に「素直に書くとこうなるが、それだと壊れる」という事情——を書く。
- コミットメッセージは、直した現象を利用者から見た言葉で書く。監査の指摘に
  対応するものは ID（`A-9` `E-3` など）を添える。**ID を書き忘れると、後から
  「直っているのに未対応に見える」**ことになる（実際に起きた）。
- モデル名・モデル ID を、コミットメッセージ・PR・コード・コメントなど
  リポジトリに入るものへ書かない。
