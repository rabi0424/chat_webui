/**
 * 数式を選択してコピーしたとき、描画結果ではなく元のTeXを渡す。
 *
 * KaTeX の出力をそのままコピーすると、記号がばらばらに並んだ読めない
 * 文字列になる。この拡張は `copy` を横取りして、選択範囲に含まれる数式を
 * MathML 側に埋めてある元のTeX（`\frac{1}{2}` など）へ置き換える。
 * 別のチャットやエディタへ数式を持っていくときに効く。
 *
 * `document` に対して直接リスナーを張るのでブラウザ専用。`.client.ts`
 * にしてサーバー側のビルドからは外している。
 */
import "katex/contrib/copy-tex";
