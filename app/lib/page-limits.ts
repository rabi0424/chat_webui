/**
 * 取り込んだページの本文に入れる印。
 *
 * 本文を作る側（`page-extract.client.ts`）と、それを検査する側が同じ
 * 文字を見る。上限そのものは設定（`AppSettings.pageMaxChars`）に移した
 * ——端末をまたいで効いてほしく、そのまま課金にも効くため。
 *
 * このファイルを `.client` から分けているのは、入力欄が取り込みの部品を
 * 静的に読まないようにするため（貼られたときに初めて読み込む）。
 */

/** 長さの上限で切ったことを、本文の末尾に書く印。 */
export const TRUNCATED_MARK = "…（長すぎるため、ここで切りました）";
